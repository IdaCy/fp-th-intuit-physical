# compute notes

All work in this contribution ran on the local machine.

The physical-only single-capability subset contains 48 item instances. That workload is small enough that remote compute is unnecessary for reproducibility or throughput.

The Vast instance was checked during setup and remains available through direct SSH if needed later, but it was not used for this benchmark run.
